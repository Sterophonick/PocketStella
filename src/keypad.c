#include <libheart.h>
#include "cpu.h"
//0-F

void Keypad()
{
	
}

void MiscKeys()
{
	if(keyDown(KEY_L))
	{
		if(keyDown(KEY_A))
		{
		
		}
		if(keyDown(KEY_B))
		{
		
		}
		if(keyDown(KEY_START))
		{
		
		}
		if(keyDown(KEY_SELECT))
		{
		
		}
	}
}